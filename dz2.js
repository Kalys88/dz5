// function countChar(str) {
//    console.log('CHAR ' + str + ' has length: ', str.length);
// }
//
// let str = 'string'
// countChar(str)
// countChar('boolean')

let countChar = (str) => {
   console.log(`CHAR ${str} ${str.length}`);
}

let str = 'string'
countChar(str)
countChar('boolean')